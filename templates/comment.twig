<div class="blog-comment {{comment.comment_type}}" id="blog-comment-{{comment.ID}}">
	<h5 class="comment-author p-t-15"><span class="text-uppercase">{{comment.author.name}} </span>says</h5>
	<div class="comment-content">{{comment.comment_content|wpautop}}</div>

	<section class="comment-box">
		<!-- child comments -->
		{#{% if post.comments %}#}
			{#<h4> replies </h4>#}
			{#<div class="comments">#}
				{#{% for cmt in comment.children %}#}
					{#{% include "comment.twig" with {comment:cmt} %}#}
				{#{% endfor %}#}
			{#</div>#}
		{#{% endif %}#}

		<!-- comment form -->
		{#{% include "comment-form.twig" %}#}
	</section>
</div>